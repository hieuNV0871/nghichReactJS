import React from 'react';
import PropTypes from 'prop-types';

const Sidebar = props => {
    return (
        <div>
            sidebar
        </div>
    );
};

Sidebar.propTypes = {
    
};

export default Sidebar;